<UserControl x:Class="TimeTracker.SettingsBlacklist"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeTracker"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Label Content="App Blacklist" HorizontalAlignment="Left" Margin="31,1,0,0" VerticalAlignment="Top" FontSize="24" FontWeight="Bold"/>
        <Label Content="Type in the names of all apps that should be ignored by TimeTracker" HorizontalAlignment="Left" Margin="31,48,0,0" VerticalAlignment="Top" FontSize="18"/>
        <DataGrid Name="DataGrid" HorizontalAlignment="Left" Height="503" Margin="31,87,0,0" VerticalAlignment="Top" Width="441" ColumnWidth="*" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"  Width="*" Binding="{Binding Name, Mode=TwoWay}" IsReadOnly="False"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Restore Defaults" HorizontalAlignment="Left" Margin="502,87,0,0" VerticalAlignment="Top" Width="230" Height="54" FontSize="20" Click="Button_Click"/>
    </Grid>
</UserControl>
